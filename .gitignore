# Dependencies / build
node_modules/
dist/

# Secrets — never commit
.env
.env.local
.tekion-token.json

# Saved browser session & captured traffic (auth tokens + customer PII)
auth.json
tekion-auth.json
captured/
*.har

# Generated documents (not part of the codebase)
*.docx
*.zip

# Error screenshots from Playwright runs
error-*.png
misc-filled-*.png
misc-submitted-*.png
misc-vendor-fail-*.png
dealer-menu-debug-*.png

# Credentials / service account keys
neura_vertex_ai.json

# Python cache
__pycache__/
*.pyc

# IDE / agent config
.agents/
.claude/

# Uploads, sample files, OCR results (data, not code)
uploads/
main_pipeline/Sample files/
main_pipeline/results/

# Internal notes
notes.md
architecture_decision.txt

# OS / editor
.DS_Store

# Every environment file except the documented template. .env.production holds
# live database credentials; an un-ignored variant is how those reach a public
# repository.
.env
.env.*
!.env.example
